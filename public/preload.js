// Preload script for Electron security
// This script runs in the renderer process before the main app loads
// It provides a secure bridge between the renderer and main processes

window.electronAPI = {
  // You can add IPC methods here if needed in the future
};
